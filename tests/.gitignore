# Test runs keep their artifacts outside the plugin (see lib/artifacts.js), so there should be
# nothing to ignore here. These patterns only catch leftovers from older versions of the tests.
.chrome/
.results-*.json
.site-backup.json
